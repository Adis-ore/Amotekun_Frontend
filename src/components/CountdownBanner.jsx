// Countdown runs silently in the background — no visible UI rendered.
// Portal open/closed state is managed independently in each page component.
export default function CountdownBanner() {
  return null
}
